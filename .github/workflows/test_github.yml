name: 'GitHub'
on:
  workflow_call:
    inputs:
      ref:
        type: string
        required: true
env:
  OTEL_SHELL_CONFIG_GITHUB_IS_TEST: 'TRUE'
jobs:
  job-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  job-node:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1 # check if node action works
        with:
          name: packages.deb.${{ inputs.ref }}
  job-docker:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: actions/hello-world-docker-action@8bcd8e1af3c095561f1043123848fc8b2db0f189 # main # check if docker action works
      - uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0 # some docker actions have otel built-in
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0 # some docker actions have otel built-in
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - run: cp opentelemetry-shell_*_*.deb tests
      - uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0 # some docker actions have otel built-in
        with:
          context: ./tests/
          tags: test
          push: false
          build-args: |
            image=ubuntu:latest
            update=TRUE
            shell=bash
  job-run:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: sudo apt-get update # check if command fails
      - run: nohup sleep $((60 * 60 * 2)) 1> /dev/null 2> /dev/null &
      - run: | # check if install succeeded
          type otel.sh && type strace
      - run: | # check if instrumentation happened
          [ "$(alias | grep -c '_otel_observe')" -gt 0 ]
      - run: | # check if traceparent is injected
          [ -n "$TRACEPARENT" ]
  job-composite:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: plengauer/autoversion@badee98ba4eb3a38f99cf8acab7f5510e7e8736f # v5.0.0
        with:
          github_token: ${{ github.token }}
          path_exclude: ./
          working_directory: ./autoversion
          dry_run: 'true'
          ref: main
  job-dotnet:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: 10
      - run: dotnet --version
  job-arm:
    needs: job-smoke
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world
  job-arm-otelcol-contrib:
    needs: job-smoke
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: |
          type otelcol-contrib
          otelcol-contrib --version
  job-integration-simulated-opt-acl-modification:
    needs: [job-smoke, job-cache]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        id: instrument
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - name: assert dependency cache was restored via the fast path
        if: steps.instrument.outputs.cache_restored_fast != 'true'
        run: |
          echo "::error::Expected the cache warmed by job-cache to be restored via the fast path, got cache_restored_fast='${{ steps.instrument.outputs.cache_restored_fast }}'."
          exit 1
      - run: touch /opt/helloworld
  job-integration-renovate:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        run: [1, 2, 3]
      fail-fast: false
      max-parallel: 1
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: renovatebot/github-action@37beffda261423addd537c33f2d126df7f6ffbab # v46.2.6 # node action with call to docker, docker flags that make injection more complicated, then call a node script via exec that already is injected with otel
        env:
          RENOVATE_DRY_RUN: full
          RENOVATE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RENOVATE_REPOSITORIES: ${{ github.repository }}
  job-integration-codeql:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9 # node action will call java will call node
        with:
          languages: javascript-typescript
          build-mode: none
      - uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9 # node action will call java will call node
        with:
          category: "/language:javascript-typescript"
          upload: never
      - uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          languages: python
          build-mode: none
      - uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9 # node action will call java will call python
        with:
          category: "/language:python"
          upload: never
  job-integration-superlinter:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: super-linter/super-linter@4ce20838b8ab83717e78138c5b3a1407148e0918 # v8.7.0
        env:
          GITHUB_TOKEN: ${{ github.token }}
          VALIDATE_ALL_CODEBASE: 'true'
          DISABLE_ERRORS: 'false'
          MULTI_STATUS: 'false'
          ENABLE_GITHUB_PULL_REQUEST_SUMMARY_COMMENT: 'false'
          # RUN_LOCAL: 'true'
          # DEFAULT_BRANCH: main
          # LOG_LEVEL: DEBUG
  job-integration-megalinter:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: oxsecurity/megalinter@15e5b45552097e318c93de385779ce3b1084052c # v10.0.0
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ github.token }}
          VALIDATE_ALL_CODEBASE: 'true'
  job-integration-python-venv:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: |
          # initializing a python venv because venv have a lot of corner cases about injection, users, file permissions, changing paths and on-the-fly instrumentation
          python3 -m venv venv
          . venv/bin/activate
          pip3 install -r src/usr/share/opentelemetry_shell/agent.instrumentation.python/requirements.txt
          deactivate
  job-integration-checkout-submodules:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: repo_with_submodules
          repository: robocorp/example-use-git-submodule-for-shared-code
          submodules: recursive
  job-integration-copilot-install:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - uses: github/gh-aw-actions/setup@bc56a0cad2f450c562810785ef38649c04db812a
      - run: bash "${RUNNER_TEMP}/gh-aw/actions/install_copilot_cli.sh" 1.0.40
  job-io-1:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    outputs:
      foo: ${{ steps.my-step.outputs.foo }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo foo=bar >> "$GITHUB_OUTPUT"
        id: my-step
      - run: |
          [ ${{ steps.my-step.outputs.foo }} = bar ]
  job-io-2:
    needs: job-io-1
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: |
          [ ${{ needs.job-io-1.outputs.foo }} = bar ]
  job-background:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: |
          ( { echo foo; sleep $((60 * 60 * 60)); echo bar; } ) &
  job-background-indirect:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: |
          echo "( { echo foo; sleep \$((60 * 60 * 60)); echo bar; } ) &" > start_background.sh
          sh ./start_background.sh
  job-cache:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        cache: ['false', 'true']
      fail-fast: false
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: ${{ matrix.cache }}
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world
  job-exporter-local:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - run: |
          nohup node -e "require('http').createServer(function (req, res) { console.log(req.method, req.url); res.writeHead(200); res.end(); }).listen(8080);" 1> /tmp/http.log 2> /dev/null &
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
          secrets_to_redact: '{"github.token": "${{ github.token }}", "url": "https://example.com/foo/bar", "regex": "[a-zA-Z]*", "quote": "foo\"bar"}'
        env:
          OTEL_LOGS_EXPORTER: otlp
          OTEL_METRICS_EXPORTER: otlp
          OTEL_TRACES_EXPORTER: otlp
          OTEL_EXPORTER_OTLP_LOGS_ENDPOINT: http://localhost:8080/v1/logs
          OTEL_EXPORTER_OTLP_METRICS_ENDPOINT: http://localhost:8080/v1/metrics
          OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: http://localhost:8080/v1/traces
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world >&2
      - run: sleep 60
      - run: cat /tmp/http.log | sort -u
      - run: cat /tmp/http.log | grep -q '^POST /v1/logs$'
      - run: cat /tmp/http.log | grep -q '^POST /v1/metrics$'
      - run: cat /tmp/http.log | grep -q '^POST /v1/traces$'
  job-exporter-global:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        exporter_logs: ['otlp', 'console']
        exporter_metrics: ['otlp', 'console']
        exporter_traces: ['otlp', 'console']
        protocol: ['http/protobuf', 'http/json']
      fail-fast: false
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - run: |
          nohup node -e "
          const fs = require('fs');
          const zlib = require('zlib');
          require('http').createServer(function (req, res) {
            console.log(req.method, req.url);
            const chunks = [];
            req.on('data', chunk => chunks.push(chunk));
            req.on('end', () => {
              let buffer = Buffer.concat(chunks);
              if (req.headers['content-encoding'] === 'gzip') { try { buffer = zlib.gunzipSync(buffer); } catch(e) {} }
              fs.appendFileSync('/tmp/' + req.url.split('/').pop() + '.json', buffer.toString() + '\n');
              res.writeHead(200);
              res.end();
            });
          }).listen(8080);
          " 1> /tmp/http.log 2> /dev/null &
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
          secrets_to_redact: '{"github.token": "${{ github.token }}", "url": "https://example.com/foo/bar", "regex": "[a-zA-Z]*"}'
        env:
          OTEL_LOGS_EXPORTER: ${{ matrix.exporter_logs }}
          OTEL_METRICS_EXPORTER: ${{ matrix.exporter_metrics }}
          OTEL_TRACES_EXPORTER: ${{ matrix.exporter_traces }}
          OTEL_EXPORTER_OTLP_PROTOCOL: ${{ matrix.protocol }}
          OTEL_EXPORTER_OTLP_ENDPOINT: http://localhost:8080
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world >&2
      - run: sleep 60
      - run: cat /tmp/http.log | sort -u
      - run: cat /tmp/http.log | grep -q '^POST /v1/logs$'
        if: ${{ matrix.exporter_logs == 'otlp' }}
      - run: |
          cat /tmp/logs.json
          logs_with_span_id=$(cat /tmp/logs.json | jq -s '[.[] | .resourceLogs[]?.scopeLogs[]?.logRecords[]? | select(.spanId != "" and .spanId != null)] | length')
          logs_with_trace_id=$(cat /tmp/logs.json | jq -s '[.[] | .resourceLogs[]?.scopeLogs[]?.logRecords[]? | select(.traceId != "" and .traceId != null)] | length')
          all_logs=$(cat /tmp/logs.json | jq -s '[.[] | .resourceLogs[]?.scopeLogs[]?.logRecords[]?] | length')
          echo "Logs with span ID: $logs_with_span_id"
          echo "Logs with trace ID: $logs_with_trace_id"
          echo "All logs: $all_logs"
          [ "$logs_with_span_id" -ge 1 ] || { echo "Expected at least 1 log with span ID"; exit 1; }
          [ "$logs_with_trace_id" -ge 1 ] || { echo "Expected at least 1 log with trace ID"; exit 1; }
        if: ${{ matrix.exporter_logs == 'otlp' && matrix.protocol == 'http/json' }}
      - run: cat /tmp/http.log | grep -q '^POST /v1/metrics$'
        if: ${{ matrix.exporter_metrics == 'otlp' }}
      - run: |
          cat /tmp/metrics.json
          job_metrics=$(cat /tmp/metrics.json | jq -s '[.[] | .resourceMetrics[]?.scopeMetrics[]?.metrics[]? | select(.name | startswith("cicd"))] | length')
          step_metrics=$(cat /tmp/metrics.json | jq -s '[.[] | .resourceMetrics[]?.scopeMetrics[]?.metrics[]? | select(.name | contains("step"))] | length')
          all_metrics=$(cat /tmp/metrics.json | jq -s '[.[] | .resourceMetrics[]?.scopeMetrics[]?.metrics[]?] | length')
          echo "Job metrics: $job_metrics"
          echo "Step metrics: $step_metrics"
          echo "All metrics: $all_metrics"
          [ "$job_metrics" -ge 1 ] || { echo "Expected at least 1 job metric"; exit 1; }
          [ "$step_metrics" -ge 1 ] || { echo "Expected at least 1 step metric"; exit 1; }
        if: ${{ matrix.exporter_metrics == 'otlp' && matrix.protocol == 'http/json' }}
      - run: cat /tmp/http.log | grep -q '^POST /v1/traces$'
        if: ${{ matrix.exporter_traces == 'otlp' }}
      - run: |
          cat /tmp/traces.json
          job_span_count=$(cat /tmp/traces.json | jq -s '[.[] | .resourceSpans[]?.scopeSpans[]?.spans[]? | select(.attributes[]? | select(.key == "github.actions.type" and .value.stringValue == "job"))] | length')
          step_span_count=$(cat /tmp/traces.json | jq -s '[.[] | .resourceSpans[]?.scopeSpans[]?.spans[]? | select(.attributes[]? | select(.key == "github.actions.type" and .value.stringValue == "step"))] | length')
          all_spans=$(cat /tmp/traces.json | jq -s '[.[] | .resourceSpans[]?.scopeSpans[]?.spans[]?] | length')
          empty_parent_count=$(cat /tmp/traces.json | jq -s '[.[] | .resourceSpans[]?.scopeSpans[]?.spans[]? | select(.parentSpanId == "" or .parentSpanId == null)] | length')
          echo "Job spans: $job_span_count"
          echo "Step spans: $step_span_count"
          echo "All spans: $all_spans"
          echo "Spans without parent: $empty_parent_count"
          # [ "$job_span_count" -ge 1 ] || { echo "Expected at least 1 job span"; exit 1; } # job span is only flushe don end of job, so not available yet
          [ "$step_span_count" -ge 1 ] || { echo "Expected at least 1 step span"; exit 1; }
          # [ "$empty_parent_count" -eq 1 ] || { echo "Expected exactly 1 span without parent (root span), found $empty_parent_count"; exit 1; } # same reason as above
        if: ${{ matrix.exporter_traces == 'otlp' && matrix.protocol == 'http/json' }}
  job-exporter-deferred:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
        env:
          OTEL_LOGS_EXPORTER: deferred
          OTEL_METRICS_EXPORTER: deferred
          OTEL_TRACES_EXPORTER: deferred
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo "hello world" >&2
  job-permissions-restricted:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions: # according to https://docs.github.com/en/actions/security-for-github-actions/security-guides/automatic-token-authentication
      actions: read
      attestations: none
      checks: none
      contents: read
      deployments: none
      discussions: none
      id-token: none
      issues: none
      # models: none
      packages: read
      pages: none
      pull-requests: none
      security-events: none
      statuses: none
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world >&2
  job-selfmonitoring:
    needs: job-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        self_monitoring: ['false', 'true', 'auto']
        self_monitoring_anonymize: ['false', 'true', 'auto']
      fail-fast: false
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - run: |
          nohup node -e "require('http').createServer(function (req, res) { console.log(req.method, req.url); res.writeHead(200); res.end(); }).listen(8080);" 1> /tmp/http.log 2> /dev/null &
      - uses: ./actions/instrument/job
        with:
          cache: 'false'
          self_monitoring: ${{ matrix.self_monitoring }}
          self_monitoring_anonymize: ${{ matrix.self_monitoring_anonymize }}
        env:
          OTEL_EXPORTER_OTLP_ENDPOINT: http://localhost:8080
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
      - run: echo hello world
  job-windows:
    needs: job-smoke
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      matrix:
        runner: [windows-2022, windows-latest]
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
      - run: echo hello world
  job-macos:
    needs: job-smoke
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      matrix:
        runner: [macos-15, macos-26, macos-latest]
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - run: |
          echo "SHELL=$SHELL"
          echo "shell=$0"
          ps -p $$ -o command=
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
      - run: echo hello world
      - shell: zsh {0}
        run: echo hello world
  job-ubuntu-slim:
    needs: job-smoke
    runs-on: ubuntu-slim
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/job
        env:
          OTEL_SHELL_CONFIG_GITHUB_INJECT_ON_MAIN: 'TRUE'
      - run: echo hello world
  workflow-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - id: workflow_run
        run: |
          curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/actions/workflows'?per_page=100&page=1' | jq --unbuffered -r '.workflows[] | select(.name == "Workflow Example") | .id' \
            | xargs -I {} curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/actions/workflows/'{}'/runs'?status=success&branch=main&per_page=1&page=1' \
            | jq --unbuffered -r '.workflow_runs[] | [.id, .run_attempt] | @tsv' | sed 's/\t/ /g' | {
              read -r id run_attempt
              echo "id=$id" >> "$GITHUB_OUTPUT"
              echo "run_attempt=$run_attempt" >> "$GITHUB_OUTPUT"
            }
      - run: |
          [ -n "${{ steps.workflow_run.outputs.id }}" ]
          [ -n "${{ steps.workflow_run.outputs.run_attempt }}" ]
      - uses: ./actions/instrument/workflow
        with:
          workflow_run_id: ${{ steps.workflow_run.outputs.id }}
          workflow_run_attempt: ${{ steps.workflow_run.outputs.run_attempt }}
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  workflow:
    needs: workflow-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      matrix:
        self_monitoring: ['false', 'true', 'auto']
        self_monitoring_anonymize: ['false', 'true', 'auto']
      fail-fast: false
    permissions:
      contents: read
      actions: read
    steps:
      - id: filename
        run: echo "otlp=$(mktemp)" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - id: workflow_run
        run: |
          curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/actions/workflows'?per_page=100&page=1' | jq --unbuffered -r '.workflows[] | select(.name == "Workflow Example") | .id' \
            | xargs -I {} curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/actions/workflows/'{}'/runs'?status=success&branch=main&per_page=1&page=1' \
            | jq --unbuffered -r '.workflow_runs[] | [.id, .run_attempt] | @tsv' | sed 's/\t/ /g' | {
              read -r id run_attempt
              echo "id=$id" >> "$GITHUB_OUTPUT"
              echo "run_attempt=$run_attempt" >> "$GITHUB_OUTPUT"
            }
      - run: |
          [ -n "${{ steps.workflow_run.outputs.id }}" ]
          [ -n "${{ steps.workflow_run.outputs.run_attempt }}" ]
      - uses: ./actions/instrument/workflow
        with:
          debug: 1
          workflow_run_id: ${{ steps.workflow_run.outputs.id }}
          workflow_run_attempt: ${{ steps.workflow_run.outputs.run_attempt }}
          self_monitoring: ${{ matrix.self_monitoring }}
          self_monitoring_anonymize: ${{ matrix.self_monitoring_anonymize }}
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: '${{ steps.filename.outputs.otlp }}'
      - run: |
          . ./actions/instrument/shared/github.sh
          INPUT_GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }} gh_artifact_download '${{ steps.workflow_run.outputs.id }}' '${{ steps.workflow_run.outputs.run_attempt }}' 'opentelemetry_workflow_run_${{ steps.workflow_run.outputs.run_attempt }}' .
      - env:
          OTLP_JSON: '${{ steps.filename.outputs.otlp }}'
        run: |
          [ "$(cat "$OTLP_JSON" | jq -r .context.trace_id | grep -v '^null$' | sort -u | cut -d x -f 2 | tee /dev/stderr)" = "$(cat traceparent | cut -d - -f 2 | tee /dev/stderr)" ]
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'test (foo)'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'test (bar)'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'test (baz)'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'publish'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'name-with_special-characters'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'job-with(a)special[name]with.special&characters'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'Set up job'
          cat "$OTLP_JSON" | jq -r .name | grep -v '^null$' | sort -u | grep -F 'Complete job'
          # cat "$OTLP_JSON" | jq -r .body | grep -v '^null$' | sort -u | grep -F 'Cleaning up orphan processes'
          # cat "$OTLP_JSON" | jq -r .body | grep -v '^null$' | sort -u | grep -F 'hello world, im a publisher (not injected)'
          # cat "$OTLP_JSON" | jq -r .body | grep -v '^null$' | sort -u | grep -F 'hello world, im a job with some special characters (not injected)'
          # cat "$OTLP_JSON" | jq -r .body | grep -v '^null$' | sort -u | grep -F 'hello world, im a job with custom name (not injected)'
  checksuite-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - id: check_suite
        run: |
          curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/branches'?per_page=100&page=1' | jq '.[].name' -r \
           | xargs -I '{}' curl -L --no-progress-meter --fail --retry 16 -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/commits/'{}'/check-suites'?per_page=100&page=1' | jq --unbuffered '.check_suites[] | select(.app.slug == "github-actions" | not) | .id' -r | tail -n 1 | {
            read -r id
            echo "id=$id" >> "$GITHUB_OUTPUT"
          }
      - run: |
          [ -n "${{ steps.check_suite.outputs.id }}" ]
      - uses: ./actions/instrument/checksuite
        with:
          check_suite_id: ${{ steps.check_suite.outputs.id }}
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-pull-request-opened:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: pull_request
          event_action: opened
          event_body: '{"pull_request":{"created_at":"2024-01-01T00:00:00Z","head":{"label":"user:branch","sha":"abc123"},"base":{"label":"user:main","sha":"def456"}}}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-pull-request-closed:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: pull_request
          event_action: closed
          event_body: '{"pull_request":{"created_at":"2024-01-01T00:00:00Z","closed_at":"2024-01-02T00:00:00Z","head":{"label":"user:branch","sha":"abc123"},"base":{"label":"user:main","sha":"def456"}}}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-pull-request-merged:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: pull_request
          event_action: closed
          event_body: '{"pull_request":{"created_at":"2024-01-01T00:00:00Z","merged_at":"2024-01-02T00:00:00Z","merge_commit_sha":"abc123","head":{"label":"user:branch","sha":"abc123"},"base":{"label":"user:main","sha":"def456"}}}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-pull-request-reopened:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: pull_request
          event_action: reopened
          event_body: '{"pull_request":{"head":{"label":"user:branch","sha":"abc123"},"base":{"label":"user:main","sha":"def456"}}}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-pull-request-review-submitted:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: pull_request_review
          event_action: submitted
          event_body: '{"review":{"state":"approved","submitted_at":"2024-01-02T00:00:00Z"},"pull_request":{"created_at":"2024-01-01T00:00:00Z"}}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-create:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: create
          event_body: '{"ref_type":"branch"}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-delete:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: delete
          event_body: '{"ref_type":"branch"}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  repository-push:
    needs: repository-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: packages.deb.${{ inputs.ref }}
      - uses: ./actions/instrument/repository
        with:
          event_name: push
          event_body: '{"ref":"refs/heads/main"}'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  deploy-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref }}
      - uses: ./actions/instrument/deploy
        with:
          debug: 1
          dry_run: true
          action_repository: '.'
          __repository_level_instrumentation_file_name_override: '.github/workflows/test_github.yml'
        env:
          OTEL_METRICS_EXPORTER: console
          OTEL_LOGS_EXPORTER: console
          OTEL_TRACES_EXPORTER: console
          OTEL_SHELL_SDK_OUTPUT_REDIRECT: /dev/null
  deploy:
    if: ${{ !github.event.repository.fork }}
    needs: deploy-smoke
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    strategy:
      matrix:
        secret_redaction_strategy: ['none', 'list', 'all']
        private: ['false']
      fail-fast: false
    env:
      REPOSITORY_TEMPLATE: thoth-deploy-test-repository-${{ matrix.private  }}-${{ matrix.secret_redaction_strategy }}
      ASSERT_RECEIVING_DATA: 'false'
    steps:
      - uses: plengauer/opentelemetry-github/actions/instrument/job@74b46584d99a285f5f5300264c94538dcf6e9596 # v5.62.0
        with:
          secrets_to_redact: '["${{ github.token }}","${{ secrets.TEST_GITHUB_TOKEN }}"]'
        env:
          OTEL_EXPORTER_OTLP_ENDPOINT: '${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}'
          OTEL_EXPORTER_OTLP_HEADERS: '${{ secrets.OTEL_EXPORTER_OTLP_HEADERS }}'
      - id: config
        run: |
          curl -L -H "Authorization: Bearer ${{ secrets.TEST_GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/user | jq -r .login | xargs -I '{}' echo "user={}" >> "$GITHUB_OUTPUT"
          echo "repository=$(echo "${{ env.REPOSITORY_TEMPLATE }}-${{ github.workflow }}-${{ github.ref_name }}" | tr / - | cut -c -100)" >> "$GITHUB_OUTPUT"
      - run: |
          curl -L -H "Authorization: Bearer ${{ secrets.TEST_GITHUB_TOKEN }}" -X DELETE "${GITHUB_API_URL:-https://api.github.com}"/repos/${{ steps.config.outputs.user }}/${{ steps.config.outputs.repository }} || true
      - run: |
          curl -L -H "Authorization: Bearer ${{ secrets.TEST_GITHUB_TOKEN }}" "${GITHUB_API_URL:-https://api.github.com}"/user/repos -d '{"name":"${{ steps.config.outputs.repository }}","private":${{ matrix.private }},"auto_init":true}'
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: "${{ steps.config.outputs.user }}/${{ steps.config.outputs.repository }}"
          token: ${{ secrets.TEST_GITHUB_TOKEN }}
      - run: |
          git config --global user.email ${{ steps.config.outputs.user }}@github.com
          git config --global user.name ${{ steps.config.outputs.user }}
      - run: |
          mkdir -p .github/workflows
          echo '
          name: Test

          on:
            push:

          jobs:

            hello-world:
              runs-on: ubuntu-latest
              steps:
                - run: echo hello world
          ' >> .github/workflows/test.yml
          git add . && git commit --message "added workflow" && git push
      - if: ${{ env.ASSERT_RECEIVING_DATA == 'true' }}
        run: |
          nohup node -e "require('http').createServer(function (req, res) { console.log(req.method, req.url); res.writeHead(200); res.end(); }).listen(8080);" 1> /tmp/http.log 2> /dev/null &
      - run: |
          printf '%s' '${{ secrets.TEST_GITHUB_TOKEN }}' | GH_TOKEN='${{ secrets.TEST_GITHUB_TOKEN }}' gh secret set DEPLOY_OBSERVABILITY_TOKEN --repo ${{ steps.config.outputs.user }}/${{ steps.config.outputs.repository }}
          wget --header "Authorization: Bearer ${{ github.token }}" https://raw.githubusercontent.com/${{ github.repository }}/${{ github.sha }}/README.md -O - | sed -n '/```yaml/,/```/p' | grep -v '```yaml' | sed '/```/,$d' > .github/workflows/deploy_otel.yml
          cat .github/workflows/deploy_otel.yml | yq .jobs.deploy.steps[0].uses | grep -q '/actions/instrument/deploy@'
          cat .github/workflows/deploy_otel.yml | yq .jobs.deploy.steps[0].with.github_token | grep -q secrets.DEPLOY_OBSERVABILITY_TOKEN
          yq -i 'del(.jobs.deploy.steps[0].env)' .github/workflows/deploy_otel.yml
          yq -i '.jobs.deploy.steps[0].uses = "${{ github.repository }}/actions/instrument/deploy@${{ inputs.ref }}"' .github/workflows/deploy_otel.yml
          yq -i '.jobs.deploy.steps[0].with.job_level_instrumentation_secret_redaction_strategy = "${{ matrix.secret_redaction_strategy }}"' .github/workflows/deploy_otel.yml
          yq -i '.jobs.deploy.steps[0].env.OTEL_EXPORTER_OTLP_ENDPOINT = "http://'"$(curl 'https://api.ipify.org?format=json' | jq -r .ip)"':8080"' .github/workflows/deploy_otel.yml
          git add . && git commit --message "added deploy action" && git push
      - run: |
          while ! cat .github/workflows/test.yml | yq '.jobs.hello-world.steps[].uses' | grep -q '/actions/instrument/job@' || ! cat .github/workflows/observability_workflow.yml | yq '.jobs.export.steps[].uses' | grep -q '/actions/instrument/workflow@'; do
            sleep 10 && git pull
          done
      - run: |
          case "${{ matrix.secret_redaction_strategy }}" in
            none) cat .github/workflows/test.yml | yq '.jobs.*.steps[] | select(.uses == "*/actions/instrument/job@*") | .with.secrets_to_redact' | sort -u | grep -q null;;
            list|all) ! cat .github/workflows/test.yml | yq '.jobs.*.steps[] | select(.uses == "*/actions/instrument/job@*") | .with.secrets_to_redact' | sort -u | grep -q null;;
            *) false;;
          esac
      - if: ${{ env.ASSERT_RECEIVING_DATA == 'true' }}
        run: cat /tmp/http.log | grep -q '^POST /v1/logs$'
      - if: ${{ env.ASSERT_RECEIVING_DATA == 'true' }}
        run: cat /tmp/http.log | grep -q '^POST /v1/metrics$'
      - if: ${{ env.ASSERT_RECEIVING_DATA == 'true' }}
        run: cat /tmp/http.log | grep -q '^POST /v1/traces$'
      - run: |
          curl -L -H "Authorization: Bearer ${{ secrets.TEST_GITHUB_TOKEN }}" -X DELETE "${GITHUB_API_URL:-https://api.github.com}/repos/${{ steps.config.outputs.user }}/${{ steps.config.outputs.repository }}" || true
